Status chip shown over the bot and in the HUD whenever an element effect or system state is active. Always paired with a sound + particle; kids debug by these.

```jsx
<StatusChip status="tangled" />
<StatusChip status="rebooting" />
```
