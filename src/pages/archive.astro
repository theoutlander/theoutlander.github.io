---
import { getPosts } from "../data/hashnode";
const posts = await getPosts(50);
---
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Archive</title>
    <link rel="stylesheet" href="/src/styles/global.css" />
  </head>
  <body>
    <main class="container">
      <h1>Archive</h1>
      <ul class="archive">
        {posts.map((p:any) => (
          <li><a href={`/p/${p.slug}/`}>{p.title}</a>
            <span class="meta"> — {new Date(p.publishedAt).toLocaleDateString()}</span>
          </li>
        ))}
      </ul>
    </main>
  </body>
</html>